#include <stdio.h>
int fact(int);

void main() {
   int n,factorial;
   printf("enter a number");
   scanf("%d",&n);
   factorial=fact(n);
   printf("%d",factorial);
}
int fact(int a)
{
    int i,f=1;
    for(i=1;i<=a;i++)
    {
        f=f*i;
    }
    return f;
}
