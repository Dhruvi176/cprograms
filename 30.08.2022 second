#include <stdio.h>

int main() {
    char s[1000];
    gets(s);
    int i;
    for(i=0;s[i]!='\0';i++)
    {
        if(s[i]=='a')
        printf("A");
        else
        printf("%c",s[i]);
    }

    return 0;
}
